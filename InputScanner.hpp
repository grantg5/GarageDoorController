class InputScanner {
	public:
		// Note: When threading is implemented, the below function will be uncommented.
    	//static void * scan(void *arg);

		// Note: When threading is implemented, the below function will be deleted.
		static void scan();
};
